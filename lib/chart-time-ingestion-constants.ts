/** Keep in sync with `CHART_TIME_INGESTION_MAX_TRAINS_PER_BATCH` in backend `chart-time-ingestion.service.ts`. */
export const CHART_TIME_INGESTION_MAX_TRAINS_PER_BATCH = 80;

/** Keep in sync with `TRAIN_LIST_CHART_INGESTION_BATCH` in backend `chart-time-ingestion.service.ts`. */
export const CHART_TIME_INGESTION_TRAIN_LIST_BATCH = 50;
